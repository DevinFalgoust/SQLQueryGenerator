package devinfalgoust.sqlquerygenerator.insert;

/**
 * An Enumerated Type signifying the different types of fields
 * that may be present with an Insert statement.
 * 
 * @author Devin Falgoust
 */
public enum FieldType {
	ID, EMAIL, NAME, TEXT;
}
